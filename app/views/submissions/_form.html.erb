<%= simple_form_for(@submission) do |f| %>
  <div class="form-inputs">
    <%= f.input :roll %>
    <%= f.file_field :pages, multiple: :true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
