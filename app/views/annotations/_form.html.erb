<%= simple_form_for([@submission, @annotation]) do |f| %>
  <div class="form-inputs">
    <%= f.input :mark %>
    <%= f.input :sketch, as: :string, input_html: {id: "json"} %>
    <%= f.input :comment %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
